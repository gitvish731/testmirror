// checks (always) – status and optional text
const want = ep.expect || {};
const mustStatus = Number(want.status || 200);
const mustContain = (want.text || '').trim();

const checks = {
  'status ok': (r) => r.status === mustStatus,
};
if (mustContain) {
  checks['body has text'] = (r) =>
    String(r.body || '').indexOf(mustContain) !== -1;
}
check(res, checks, tags);
