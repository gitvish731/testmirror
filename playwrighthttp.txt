// perf/k6/sources/endpoints.byFeature.js
// Pure ESM, no CommonJS. k6 0.46 is fine with this.

const features = {
  "": [
    {
      name: "Lists_SBC_compliances for all found Release Registrations",
      method: "GET",
      url: "https://",
      // optional helpers (won’t break anything if your runner ignores them)
      headers: {},          // e.g. { "content-type": "application/json" }
      auth: null,           // e.g. "bearer"
      expect: { status: 200, text: "releaseTagStatus" }
    }
  ],

  // keep other features here as needed…
  
};

export default features;


import features from "../sources/endpoints.byFeature.js";
console.log("features loaded:", Object.keys(features || {}));
